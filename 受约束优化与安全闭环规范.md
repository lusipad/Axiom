# Axiom 受约束优化与安全闭环规范

> 状态：R6 v1 + R7-A 实现基线（Windows-only、Offline Recommendation + Synthetic Shadow Contract）
>
> 目标：在不改写 Core、不越过数学/物理/权限边界的前提下，从用户目标反向寻找可验证参数候选。

## 1. R6 不是什么

R6 不是自动调机器，也不是把若干异质量纲加成一个“智能总分”。它产出的是带证据、限制条件和后续验证计划的 `RecommendationSet`。`Recommendation` 与 `AcceptanceRecord` 永久分离；R6 v1 不提供设备写入、自动接受、Shadow 或 Closed Loop 权限。

## 2. 首版参数面

首版只处理同一条已冻结五轴几何链上的两个参数：

- `feedOverride ∈ {0.70, 0.85, 1.00}`；
- `samplePeriod ∈ {0.04, 0.08} s`。

`feedOverride` 必须通过生成降额后的 `MotionConstraintProfile` 并重新执行 F3 jerk-feasible planner 实现。线速度、加速度、jerk 上限分别按 `f`、`f²`、`f³` 缩放；禁止直接缩放已有 M4 时间戳或复用旧证书。

`samplePeriod` 的两个端点必须有独立 synthetic SIL holdout 证据。模型可计算但超出 `PhysicalApplicability` 时，结果不得进入候选域。

## 3. 目标向量与 Pareto

首版三个目标均最小化，并保持独立量纲：

1. `cycleTimeSeconds`：重新规划后 M4 的总时长；
2. `linearFollowingErrorMaxMm`：X/Y/Z 指令与物理模型响应的最大绝对差；
3. `commandSampleCount`：M5 指令样本数。

不得为三个目标配置权重或相加。候选 A 支配候选 B，当且仅当 A 的三个目标均不差于 B，且至少一个严格更好。Pareto 集之后按 `(cycleTimeSeconds, linearFollowingErrorMaxMm, commandSampleCount, feedOverride, samplePeriod)` 确定性排序。

用户可附加三个同量纲上限。用户上限只决定 `goalFeasible`；数学、碰撞、物理适用性等硬门失败的候选无论目标多好都不得进入 Pareto 集。

## 4. 七个数学硬门

每个候选必须携带并重验以下 Claim 状态：

1. `five-axis.geometry-valid-claim@1`；
2. `five-axis.task-geometry-collision-free-claim@1`；
3. `five-axis.kinematically-feasible-claim@1`；
4. `five-axis.configuration-collision-free-claim@1`；
5. `five-axis.continuously-feasible-claim@1`；
6. `five-axis.interval-certified-claim@1`；
7. `five-axis.model-collision-free-claim@1`。

前四项绑定同一 F4 基线 M0–M3 内容身份；后三项必须针对候选重新规划的 M4/M5 重算。任一状态不是 `Supported`，候选 `hardConstraintsSatisfied=false`。

## 5. 物理与学习证据

R4 多采样率适用性证据必须：

- 使用与一阶拟合模型结构不同的双滞后 oracle；
- 独立覆盖 `0.04 s` 和 `0.08 s`；
- 每个端点的线性轴 RMSE 改善率不低于 `0.35`；
- 保持 `synthetic-sil` 和 `realityValidationStatus=Open`。

R5 模型在 R6 v1 中只输出 `oodFraction` 与 `epistemicStatus`。OOD 不删除 Offline 候选，但任何非零 OOD、推理不可用或现实验证 Open 都令 `promotionEligible=false`。R5 预测不得替代 R4 物理目标，也不得升级数学真值。

## 6. Recommendation 合同

`RecommendationSet` 是 `optimization.domain-pack@1` 的唯一主 Artifact，至少包含：搜索规格、基线参数、候选、Pareto 候选 ID、多采样率证据、验证计划、权限边界和 `contentHash`。

每个候选至少包含：

- 通用 `ParameterSet`；
- M4/M5/物理响应内容身份；
- 三目标向量；
- 七个数学 Gate receipt；
- `hardConstraintsSatisfied`、`goalFeasible`、`paretoOptimal`；
- `oodFraction`、`epistemicStatus`、`promotionEligible=false`；
- 改善与牺牲的解释。

Domain runtime 必须用 `searchSpec` 确定性重放并比较完整 Artifact 内容身份。API 的搜索端点只负责生成；公共 `POST /api/v1/runs/evaluate` 负责验收，二者不得合并成不可审计的隐藏状态。

## 7. 权限与验证计划

R6 v1 的权限固定为：

- `permissionLevel=Offline`；
- `deviceWriteAllowed=false`；
- `automaticAcceptanceAllowed=false`；
- `shadowStatus=Open`；
- `realityValidationStatus=Open`。

验证计划必须声明剩余门、停止条件和回滚基线。推荐结果不得使用 `DeviceSafe`、`ProcessSafe`、`safe-to-run` 或“可上机”等表述。

## 8. 阶段退出门

R6 v1 只有在下列条件全部满足时闭合：

- 六个冻结网格候选可确定性重放；
- 至少一个候选通过七个数学硬门和用户约束；
- Pareto 集、解释、哈希与顺序可复现；
- Python、HTTP、公共 Run 对同一请求语义一致；
- Optimization Lab 不暴露设备写入或自动接受入口；
- Windows AMD64 / CPython 3.12.10 验收通过；
- 所有结果继续明确标注 reality validation open、not device safe。

## 9. R7-A 的目标与边界

R7-A 不是把 R6 推荐接到机床，而是先冻结 Controlled Runtime 的权限、状态机、包线、停止、回退和责任审计合同。首包只执行 Windows 进程内的确定性 `synthetic-shadow` replay；它不连接网络、厂商 SDK、控制器或安全 PLC，也没有参数写入、cycle start、feed hold、reset、jog、程序传输或联锁旁路能力。

R7-A 的权限上限固定为：

- `permissionCeiling=Shadow`；
- `deviceWriteAllowed=false`；
- `deviceWritePerformed=false`；
- `automaticAcceptanceAllowed=false`；
- `humanApprovalPresent=false`；
- `realDeviceAuthorityPresent=false`。

这里的 Shadow 只证明软件合同和故障闭锁可以确定性重放，不代表真实设备 shadow 已完成。`deploymentShadowStatus`、`controlledTrialStatus` 和 `closedLoopStatus` 均保持 `Open`。

## 10. R7-A 核心对象

R7-A 使用独立 `control.domain-pack@1`，一次 Run 的主 Artifact 为 `axiom.control.runtime-audit@1`。R6 `RecommendationSet` 是只读上下文，不被 R7 改写。

| 对象 | 职责 | 不允许承担的语义 |
|---|---|---|
| `RuntimeSpec` | 绑定场景、请求权限、候选、Recommendation 内容身份、包线和 shadow trace | 隐式设备授权 |
| `ControlEnvelope` | 冻结线性误差、OOD、监控间隔、停止策略和回退基线 | 跨设备通用安全包线 |
| `AcceptanceRecord` | 独立记录处置、责任方、策略和证据快照 | 修改 Recommendation 或替代真实人工批准 |
| `ControlledRuntimeAudit` | 保存入场、状态迁移、监控、停止/回退 receipt 和阶段状态 | `DeviceSafe`、`ProcessSafe`、上机许可或标准合规声明 |

`AcceptanceRecord.contentHash` 必须独立于 `RecommendationSet.contentHash`。证据快照可以引用 Recommendation 的不可变身份，但不能让 Optimizer 自行批准自己的输出。

## 11. 入场、状态机和 fail-closed 语义

首版状态机固定为：

```text
Prepared → Admitted → Monitoring → Completed
    │                         └→ StopRequested → Stopped → RollbackVerified
    └→ Blocked
```

以下任一条件成立必须在入场时进入 `Blocked`，不得启动监控：

- 请求权限超过 `Shadow`；
- 请求任何设备写入；
- 请求 deployment shadow 但缺少真实部署证据；
- Recommendation 候选不存在或数学/物理硬门失败；
- 内容身份、状态迁移或支持对象被篡改。

监控过程中，线性跟随误差、OOD fraction 或监控间隔任一超出 `ControlEnvelope` 时，必须走完整停止路径。R7-A 的 `StopReceipt.effect=PromotionSuppressed` 只表示停止 shadow replay 并禁止晋级；`deviceStopCommandIssued=false`、`deviceAcknowledged=false` 必须永久记录，禁止把它称为机床急停。

同理，`RollbackReceipt.status=BaselineRetained` 只证明本平台从未改写离线基线；`deviceWriteIssued=false`、`deviceReadbackVerified=false`。它不是实际设备参数回写或读回验证。

## 12. 指标、Claim 与场景

R7-A 只发布以下受限 Claim：

- `control.runtime-audit-integrity-claim@1`；
- `control.admission-policy-enforced-claim@1`；
- `control.no-device-write-boundary-claim@1`；
- `control.shadow-stop-path-claim@1`；
- `control.shadow-rollback-boundary-claim@1`；
- `control.deployment-readiness-claim@1`。

前五项只证明 synthetic shadow 软件合同；`control.deployment-readiness-claim@1` 在没有真实部署证据时必须为 `Inconclusive`。内建场景覆盖 nominal、包线越界、deployment evidence 缺失、Controlled Trial 越权和设备写请求阻断。

## 13. R7-A 退出门与未闭合条件

R7-A 合同片只有在下列条件全部满足时闭合：

- 五个冻结场景可确定性重放且内容身份稳定；
- nominal 进入 `Completed`，包线越界进入 `RollbackVerified`；
- 越权、缺真实证据和设备写请求均进入 `Blocked`；
- Python、HTTP 和公共 Run 对同一 audit 得到一致状态；
- Controlled Runtime UI 不提供任何设备操作控件；
- Windows AMD64 / CPython 3.12.10 的测试、构建和 wheel 安装后 smoke 通过；
- 所有输出继续标记 deployment/Controlled Trial/Closed Loop Open、not a device safety claim。

完整 R7 退出门仍需具体部署提供：真实 shadow Observation、版本化厂商 Adapter、认证主体与最小权限、设备侧停止确认、参数回退和读回确认、独立安全验证、责任方签署及适用法规证据。在这些条件到位前，不得把 R7-A 合同片写成“R7 已闭合”。

## 14. 工业安全参考边界

R7-A 参考 [ISO 16090-1:2022](https://www.iso.org/standard/81558.html) 的机床危害与防护范围、[ISO 13849-1:2023](https://www.iso.org/standard/73481.html) 的安全相关控制系统设计方法、[ISO 13849-2:2012](https://www.iso.org/standard/53640.html) 的分析/测试验证边界，以及 [IEC 60204-1:2016](https://webstore.iec.ch/en/publication/26037) 对机器电气设备、STO 和急停相关要求的范围说明。这些引用用于证明为什么软件 shadow receipt 不能替代具体设备安全功能；当前实现没有进行标准符合性评估，`standardsComplianceStatus=NotAssessed`。
