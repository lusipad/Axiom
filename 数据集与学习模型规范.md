# 数据集与学习模型规范

> 状态：R5-A Windows synthetic learning 合同片已实现；realWorldGeneralizationStatus=Open
> 文档入口：[README](README.md)
> 上游依赖：[物理模型与现实对齐规范](物理模型与现实对齐规范.md)、[ADR-0015](架构决策记录/ADR-0015-R4轴空间物理模型与SIL可信度边界.md)、[ADR-0016](架构决策记录/ADR-0016-R5可审计数据集与端侧模型边界.md)
> 更新日期：2026-08-12

## 1. 文档目的

本规范只定义 R5-A 的合同片，不宣称完整 R5 已闭合。R5-A 的目标是把可审计数据、可复现学习、可解释不确定性和端侧目标运行面先闭合到一个最小可信 slice，再把真实跨设备、跨工况的泛化门单独保留为后续阶段。

R5-A 允许证明的是：

- 数据集快照是可重放、可审计、可分割的；
- 训练、验证、测试之间没有谱系泄漏；
- 只读目标解释器能复现训练侧推理；
- OOD 可以优先识别，残差模型可以优先从 X 轴开始；
- 合成合同可以闭合，而真实泛化门仍然保持 Open。

R5-A 不允许证明的是：

- 设备安全；
- 过程安全；
- 自动写设备；
- 在线学习；
- 已经完成跨设备、跨工况的真实泛化闭合。

## 2. 阶段门

| 阶段状态 | 作用范围 | 允许结论 |
|---|---|---|
| `syntheticLearningContractStatus` | 冻结合成/回放数据与静态目标解释器 | `Passed` / `Failed` |
| `realWorldGeneralizationStatus` | 真实跨设备、跨工况 holdout | `Open` / `CaseScopedPassed` |

说明：

- `syntheticLearningContractStatus=Passed` 只表示 R5-A 的合同片成立，不表示真实部署、真实设备安全或全局泛化已经完成。
- `realWorldGeneralizationStatus` 只有在独立的真实 holdout、冻结许可、校准、时间和坐标上下文都到位后，才可以按 case 逐步推进。
- 在此之前，任何“现实已闭合”“设备安全已验证”“可自动写回”等说法都属于禁语。

## 3. 数据集快照

`DatasetSnapshot` 是 R5 的主数据产品，必须是不可变版本化对象。它至少绑定：

- 查询与筛选规则；
- 纳入和排除条件；
- 样本内容身份与内容哈希；
- 标签、派生与特征契约；
- 分割清单与分割哈希；
- 逐样本谱系清单；首片将其作为 `DatasetSnapshot` 的有类型内容并由 snapshot `contentHash` 一并封存；
- 治理清单；首片将其作为 `DatasetSnapshot` 的有类型子对象并由 snapshot `contentHash` 一并封存；
- 已知偏差、覆盖空洞与保留策略；
- 授权、许可、脱敏和保留期限。

数据集快照的硬约束：

- 缺少治理信息的快照必须拒绝；
- 重复样本 ID 必须拒绝；
- 任何无效 split 引用必须拒绝；
- 任何穿越 train/validation/test 的连通身份组件必须拒绝；
- 任何把后续派生样本混回 train 的做法都必须拒绝。

## 4. Split、Lineage 与 Governance

R5 使用 split / lineage / governance 三条并行边界，不允许把它们折叠成一个“目录化数据包”。

### 4.1 Split

Split 必须满足：

- 连通身份组件只允许出现在一个 split 中；
- 轨迹、任务、设备批次、来源对、采集会话、变换链和同一主体版本都属于连通身份组件候选；
- split 时间必须单调前进；
- preprocessing 只能在 train 上 fit；
- calibration 只能使用声明过的 validation 集；
- test 永远不能参与训练或校准。

### 4.2 Lineage

谱系必须可追溯到：

- 原始源文件或源记录；
- 采集/导入 runner；
- 清洗、对齐、切分和标签生成步骤；
- 训练、导出和 parity 结果；
- 外部标准映射。

R5 允许借用 OpenLineage 和 MLCommons Croissant 的词汇做交换，但不把它们变成隐式运行时依赖。Axiom 自己仍然拥有内容身份、分割和治理语义。

### 4.3 Governance

治理清单必须显式记录：

- 许可和使用范围；
- 保留与删除策略；
- 脱敏与可再分发边界；
- 人工审查或数据释放门；
- 是否允许进入训练、验证、评估或导出链。

没有治理清单的数据，不是可训练数据，只是未审计输入。

## 5. 学习顺序

R5-A 的学习顺序是固定的。

1. 先做 OOD，再谈回归值。
2. 先做 X-only 残差头，再考虑任何其他轴。
3. Y、Z、B、C 在没有新增证据前保持 `NoValidatedImprovement` 或 `InsufficientExcitation`。
4. 不得伪造“全轴已提升”的结论来填平缺口。

首个可接受的 spike 结果是：

- 第一版全轴方案未通过预声明的 20% 改善门；
- 冻结的 X-only 残差候选通过了门槛，取得 50.43% 的 in-domain RMSE 改善；
- 区间覆盖率为 83.33%；
- OOD 探针检测为 100%；
- 训练侧与目标侧的跨运行时差异约为 `1.39e-17`。

这些结果只说明 R5-A 合同片可行，不说明真实跨设备、跨工况泛化已经成立。

## 6. 不确定性与 OOD

R5-A 的不确定性采用 split conformal 的绝对残差半径。

规则如下：

- 半径必须在 group-disjoint 的 validation 集上标定；
- 只有在声明的 exchangeability 边界内，区间覆盖解释才有效；
- 一旦出现跨设备、跨工况、时间漂移或自适应采样，exchangeability 就不能默认成立；
- OOD envelope 必须显式存在；
- OOD 命中时必须弃权，不得伪造点预测结论。

换句话说，split conformal 不是“无条件置信度”，它只在边界内成立。边界外必须回到 `Inconclusive`、`Unsupported` 或 abstain。

## 7. ModelBundle 与目标解释器

`ModelBundle` 是 JSON，不是代码包，也不是 ONNX 的代名词。它必须冻结：

- 输入契约；
- 预处理；
- 权重；
- 适用域；
- 不确定性设置；
- OOD envelope；
- 资源预算；
- 训练受理回执哈希；
- 目标解释器、输入/预处理/输出合同与 parity 容差。

Parity receipt 在 bundle 完成后单向绑定最终 `ModelBundle.contentHash`。`ModelBundle` 不反向嵌入 parity receipt hash，避免形成无法计算的循环内容身份；两者的匹配关系由同一次 Run 的 typed context 与确定性重放验证。

训练实现可以使用 NumPy 或其他冻结数值栈，但目标运行面必须是独立的纯 Python 解释器，直接消费 `ModelBundle` 并复现推理，不得导入训练侧实现作为运行依赖。

ONNX 只作为后续 Adapter 候选，不作为首版锁定格式。

## 8. typed API / UI

R5-A 的 API 和 UI 必须是 typed 的。

要求：

- manifest / scenario / example / evaluate 这些接口形态必须以 OpenAPI 中的显式 response model 发布；
- CLI、HTTP 和 UI 只能消费同一份数据定义；
- UI 必须展示数据集谱系、split、覆盖空洞、残差、OOD、区间不确定性、bundle 预算和阶段状态；
- UI 不得暴露写设备、在线学习或自动部署控件。

R5-A 的目标是读、验、比，不是写、控、发。

## 9. 禁语与边界

以下词汇在 R5-A 中默认禁止出现在 claims、UI 或对外说明里，除非后续真实门已经明确闭合：

- `DeviceSafe`
- `ProcessSafe`
- `safe-to-run`
- `online learning`
- `write device`
- `writeback`
- `autonomous deployment`
- `reality validated`
- `cross-device generalization closed`

如果确实需要描述当前状态，必须使用：

- `syntheticLearningContractStatus`
- `realWorldGeneralizationStatus`
- `Open`
- `CaseScopedPassed`
- `Inconclusive`
- `Unsupported`

## 10. 参考依据

R5-A 的写法参考了以下外部工作，但不把这些工作本身当作运行时依赖：

- Model Cards，DOI `10.1145/3287560.3287596`，用于说明模型意图、适用域和限制；
- Datasheets for Datasets，DOI `10.1145/3458723`，用于说明数据来源、收集和使用边界；
- NIST synthetic manufacturing 论文，DOI `10.1109/BigData.2017.8258117`，用于说明合成制造数据和真实制造数据的边界；
- OpenLineage，用于谱系术语和跨工具交换；
- MLCommons Croissant，用于数据集元数据表达；
- split conformal 预测，用于在 exchangeability 边界内定义区间不确定性；
- ONNX，用于后续 Adapter 候选，而不是首版格式锁定。
