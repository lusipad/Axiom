# 有序离散点领域包规范

> 文档类型：领域包规范  
> 领域包：`OrderedPointSequencePack`  
> 状态：Draft / 首个实现切片 v0.2  
> 依赖：[Axiom 通用评估框架规范](Axiom%20通用评估框架规范.md)  
> 后续领域包：[FiveAxisTrajectoryPack — CNC 五轴数学参考系统全景规范](CNC%20数学参考系统全景规范.md)

## 1. 为什么从离散点开始

有序离散点足够简单，可以验证通用框架的输入、能力协商、指标、比较、证据和追溯；同时又足够真实，可以承载算法输出、采样轨迹、测量结果以及后续五轴离散命令的公共部分。

本领域包要回答一个最直接的问题：

> 如果用户只输入一些离散点，Axiom 能不能评估？

答案是：**能，但只评价当前信息足以定义的性质**。平台不会因为缺少完整物理语义而拒绝所有评估，也不会猜测单位、时间、参考曲线或点的顺序含义。

## 2. 首版范围

首版输入是**有序欧氏点序列**：

```text
P = (p0, p1, ..., p[n-1]),  pi ∈ R^d,  d ∈ {2, 3}
```

最低契约：

- 点序列的顺序由提交者给定，且属于数据语义的一部分；
- 所有点维数一致；
- 坐标为有限数；
- 原始输入不可被评估器静默重排、去重、平滑或补点；
- `n >= 1` 可以进入结构评估，但不同指标可以要求更多点。

首版不把以下对象混为同一类型：

| 对象 | 是否属于首版 | 原因 |
|---|---|---|
| `OrderedPointSequence` | 是 | 顺序明确，可定义相邻段、长度和转角 |
| `UnorderedPointSet / PointCloud` | 否 | 需要邻域、配准、重建或排序策略 |
| `SampledSignal` | 否 | 自变量、通道和采样语义不同 |
| `PoseSequence` | 否 | 姿态表示、旋转距离和同步规则属于更强契约 |
| `Curve / Surface` | 否 | 连续对象及其参数域需要独立领域定义 |

未来可通过显式 Adapter 将这些对象转换为新的有序点 Artifact；转换必须产生新的 Artifact 和 Provenance，不能篡改原始输入。

## 3. 渐进式语义

同一组点可以随着上下文增加获得更丰富的评估能力：

| 能力级 | 新增上下文 | 可启用的评估 |
|---|---|---|
| `DP0 Coordinate Geometry` | 点、给定顺序和欧氏坐标契约 | 结构有效性、点数、维数、重复/零长度段、坐标范围、开放路径长度、步长、转角、离散曲率代理 |
| `DP1 Physical Geometry` | 物理单位、坐标系、闭合语义、容差 | 物理量解释、跨对象比较、闭合路径长度、尺度相关 gate |
| `DP2 Reference` | 参考对象和对应策略 | 偏差、Hausdorff/Fréchet 类距离、分位数和 pass/fail |
| `DP3 Parameter/Time` | 参数值或时间戳及估计策略 | 参数间隔、速度、加速度、jerk 和采样稳定性 |
| `DP4 System Context` | 约束或机器 Profile、显式领域 Adapter | 轴约束、可执行性、设备/物理比较；由其他领域包实现 |

缺少更高层上下文不会让 `DP0` 结果失效。新增上下文产生新的 Case/Run，而不是回写旧结论。

若没有声明物理单位，欧氏坐标运算仍然有定义，但所有长度类结果必须标记为合成单位 `coordinate-unit`。它只表示该 Artifact 自身坐标数值的尺度，不得解释为 mm、m 或其他物理单位，也不得在未给出显式尺度变换时用于跨 Artifact 的物理比较。角度仍以 rad/deg 明确报告，离散曲率的量纲为 `1/coordinate-unit`。

## 4. 领域对象

### 4.1 `OrderedPointSequence`

概念字段如下；这不是对具体 JSON、数据库或语言类型的提前锁定：

```yaml
artifactType: ordered-point-sequence
schemaVersion: 1
points:
  - [0.0, 0.0, 0.0]
  - [1.0, 0.2, 0.0]
  - [2.0, 0.5, 0.1]
semantics:                 # 可选
  unit: mm
  coordinateFrame: workpiece
  closed: false
parameter:                 # 可选；可以是一般参数或时间
  kind: time
  unit: s
  values: [0.0, 0.01, 0.02]
```

必须区分：

- 字段未提供；
- 字段明确为未知；
- 字段值为零或空；
- 字段提供但不合法。

`coordinate-unit` 是评估器在物理单位缺失时使用的结果标记，不是提交者可以借此声称“单位已知”的物理单位。

### 4.2 `PointSequenceProfile`

Profile 保存可复用解释或约束，例如：

- 长度和时间单位；
- 坐标系标识及版本；
- 序列是否应闭合；
- 允许的重复点、最大步长或最大闭合间隙；
- 参数/时间的单调性要求；
- 数值容差策略。

Profile 不能覆盖 Artifact 中显式且冲突的声明。冲突必须报错或由显式转换解决。

### 4.3 `ReferenceBinding`

参考评估必须同时给出：

1. 参考对象；
2. 对齐或坐标变换；
3. 对应策略；
4. 距离定义；
5. 容差和边界处理。

参考对象可以是另一条点序列，也可以由其他领域包提供连续曲线、曲面或模型。领域包不得仅凭“两组点长度相同”就假设逐点对应。

首版内置对应策略必须使用版本化 ID：

| 策略 ID | 适用条件 | 确定性规则 |
|---|---|---|
| `ordered-point.correspondence.index-paired@1` | 两序列点数相同且提交者显式声明逐点对应 | 第 `i` 个点只对应第 `i` 个点 |
| `ordered-point.correspondence.nearest-directed@1` | 被测点到离散参考点集的定向距离 | 距离并列时选择最小参考索引，并记录并列数 |
| `ordered-point.correspondence.discrete-hausdorff@1` | 两个离散点集 | 两个定向最大值取较大者；并列证据索引按字典序最小 |
| `ordered-point.correspondence.discrete-frechet@1` | 两条有序离散序列 | 使用标准离散 Fréchet 动态规划；多个最优耦合时输出字典序最小索引路径 |

由其他领域包提供的连续投影或参数对应策略必须同样声明目标函数、可行域、并列处理、边界规则和版本；未绑定策略时返回 `InsufficientContext`，当前 Evaluator 未实现已绑定策略时返回 `UnsupportedCapability`。

## 5. 验证规则

### 5.1 输入有效性

首版验证器至少检查：

- 点列表存在且 `n >= 1`；
- 维数为 2 或 3，且全序列一致；
- 所有坐标有限；
- 可选参数数组长度与点数一致；
- 时间/参数在声明要求下严格递增或非递减；
- Profile 与 Artifact 的单位、坐标系和闭合语义不冲突；
- ReferenceBinding 的维数和变换可用。

重复点和零长度段默认是**可报告问题**，不是一律非法。是否构成硬失败由 Case 决定。

### 5.2 不允许的隐式修复

验证器不得自动：

- 删除 NaN/Inf 点；
- 合并重复点；
- 按坐标或最近邻重排；
- 猜测二维/三维缺失坐标；
- 归一化尺度后仍以原单位报告；
- 把点索引当作物理时间；
- 用最近点投影代替未声明的对应关系。

如需清洗，必须使用显式 Transform/Adapter，记录方法、参数和输入输出 Artifact。

### 5.3 领域失败码

领域包至少区分：

- `EmptySequence`；
- `UnsupportedDimension`；
- `InconsistentDimension`；
- `NonFiniteCoordinate`；
- `ConsecutiveDuplicate`；
- `ParameterLengthMismatch`；
- `NonMonotoneParameter`；
- `ProfileConflict`；
- `ReferenceDimensionMismatch`；
- `UndefinedCorrespondence`。

这些失败码映射到通用框架的 `ExecutionStatus`、`MetricStatus` 和 `CaseOutcome` 三条状态轴，但保留领域细节。输入结构失败不得伪装成 Runner 执行失败。

## 6. 指标目录

指标必须独立版本化，并声明前置能力、单位、算法和数值容差。

### 6.1 能力 ID 与指标依赖

本领域包的首版能力清单如下：

| 能力 ID | 成立条件 |
|---|---|
| `ordered-point.artifact.parsed@1` | 输入已解析到可检查的点列表 |
| `ordered-point.artifact.valid@1` | 满足 §5.1 的最低结构契约 |
| `ordered-point.sequence.ordered@1` | 顺序由 Artifact 类型和提交者声明固定 |
| `ordered-point.coordinate.euclidean@1` | 坐标维数为 2 或 3，距离使用欧氏范数 |
| `ordered-point.coordinate.unit-known@1` | 已声明合法物理长度单位；`coordinate-unit` 不满足此能力 |
| `ordered-point.coordinate.frame-known@1` | 已声明可解析的坐标系或显式变换 |
| `ordered-point.sequence.closed-declared@1` | `closed` 已明确声明为 true 或 false |
| `ordered-point.reference.bound@1` | ReferenceBinding 完整且兼容 |
| `ordered-point.correspondence.policy-bound@1` | 已绑定可解析的版本化对应策略 |
| `ordered-point.parameter.values@1` | 参数数组存在、长度匹配且满足声明的单调性 |
| `ordered-point.parameter.time@1` | 参数已明确为时间并带合法时间单位 |

MetricDefinition 至少按下表声明 `requires`；最低点数和零长度段等动态前置条件仍需由具体定义补充：

| 指标族 | `requires` |
|---|---|
| 点数、维数、有限性事实 | `ordered-point.artifact.parsed@1` |
| 重复点、零长度段 | `artifact.valid`、`sequence.ordered`、`coordinate.euclidean` |
| bounds、`path.length.open`、`closure.gap`、步长、转角、离散曲率 | `artifact.valid`、`sequence.ordered`、`coordinate.euclidean` |
| `path.length.closed` | 上述能力 + `sequence.closed-declared`，且值必须为 true |
| 物理尺度阈值或跨对象物理比较 | 上述能力 + `coordinate.unit-known`；跨对象时还需兼容 frame 或显式变换 |
| 参考型指标 | `reference.bound` + `correspondence.policy-bound` 及对应策略声明的坐标能力 |
| 参数间隔 | `parameter.values` |
| 速度、加速度、jerk 和时间对齐 | `parameter.time` + 指标定义声明的差分/端点策略 |

表中的短名均指同一行对应的完整 `ordered-point.*@1` ID；机器可读清单必须保存完整 ID，不得保存短名。

### 6.2 结构与输入事实

| 指标/事实 | 最低点数 | 说明 |
|---|---:|---|
| `point.count` | 1 | 点数 |
| `coordinate.dimension` | 1 | 维数 |
| `coordinate.finite` | 1 | 全部坐标是否有限 |
| `duplicate.consecutive.count` | 2 | 相邻重复点数量 |
| `segment.zero_length.count` | 2 | 在声明容差下的零长度段数量 |

### 6.3 内在几何指标

设相邻步长 `l_i = ||p_{i+1} - p_i||`。

| 指标 | 最低点数 | 说明 |
|---|---:|---|
| `bounds.axis_aligned` | 1 | 各维最小值/最大值 |
| `path.length.open` | 2 | 始终只计算相邻点段：`sum(l_i)`，不补首尾段 |
| `closure.gap` | 2 | 首尾点距离；无论是否声明闭合都可作为“若闭合所需补段”报告 |
| `path.length.closed` | 2 | 仅当 `closed=true` 时为 `path.length.open + closure.gap`；`closed=false` 时 `NotApplicable`，未声明时 `InsufficientContext` |
| `step.length.min/max/mean/rms/std` | 2 | 采样步长分布，不等同于速度 |
| `turning.angle.*` | 3 | 相邻非零段夹角统计 |
| `curvature.discrete.*` | 3 | 必须注明所用离散曲率估计器 |

内在指标可以描述“这组点本身是什么样”，不能单独证明它接近用户目标或可由机器执行。缺少物理单位时，长度、bounds、步长和曲率仍可按 `coordinate-unit` 计算；任何以 mm、m 等物理单位声明的阈值则返回 `InsufficientContext`。

### 6.4 参考型指标

参考指标的名字必须包含或绑定对应策略：

- `paired.euclidean.rms/max/quantile`：显式逐点对应；
- `nearest.directed.max/mean`：从被测点到参考对象的定向最近距离；
- `hausdorff.discrete`：两组离散点的对称 Hausdorff 距离；
- `frechet.discrete`：保留序列顺序的离散 Fréchet 距离；
- 由连续曲线/曲面领域包提供的投影误差或参数对应误差。

不同距离回答不同问题：最近距离可能隐藏顺序错误，逐点误差依赖采样对应，Hausdorff 不表达时间顺序。平台不得把它们都显示为无定义的“轮廓误差”。

### 6.5 参数与时间指标

只有提供参数/时间及其语义后，才可以计算：

- 参数间隔或采样周期的最小值、最大值、均值和抖动；
- 速度、加速度和 jerk 的离散估计；
- 约束超限次数、持续区间和峰值；
- 与参考时间线对齐后的相位或滞后指标。

差分阶数、端点处理、滤波/平滑方法必须进入 MetricDefinition。未经声明的平滑会改变被评对象，禁止作为隐藏预处理。

### 6.6 运行代价指标

当点序列是某算法运行的输出时，还可记录：

- wall-clock time；
- CPU/GPU time；
- peak memory；
- 输出点数和压缩率；
- 超时、重试和失败类型。

代价指标是 Run 的观测，不是点序列本身的属性。

## 7. Case 类型

首版至少覆盖四类 Case：

### Case A：单序列自描述

输入只有点序列。输出结构有效性和 `coordinate-unit` 下的内在几何指标。需要物理单位、参考或时间的指标根据 §6.1 的依赖分别返回 `InsufficientContext` 或 `NotApplicable`，不得把所有几何指标一并关闭。

### Case B：结果—参考比较

输入被测序列、参考对象和 ReferenceBinding。先通过各自结构 gate，再计算声明的参考型指标。

### Case C：带时间的序列

输入点、时间戳和差分策略。计算时间有效性及运动学离散指标；不能把索引默认解释为秒。

### Case D：算法 A/B

同一输入和 ParameterSet 契约下运行两个 Subject，分别保存 Run；只在指标定义、Profile 和 ReferenceBinding 兼容时比较结果。

## 8. 比较规则

两个 Run 可直接比较，至少需要：

- 相同或明确兼容的 Artifact 语义；
- 相同 Case 目标和硬门槛；
- 相同 MetricDefinition 版本；
- 相同或可证明等价的单位与坐标系；
- 相同 ReferenceBinding，或明确的归一化策略；
- 已记录数值精度、容差和执行环境差异。

不兼容结果不得强行排序。系统应指出冲突字段以及需要的 Adapter。

首个可执行策略固定为 `ordered-point.run-comparison.strict@1`。在没有已注册 Adapter 时，它要求双方完全相同的 DomainPack、Runner、Artifact 类型与 schema、坐标维数、单位、坐标系、闭合语义、参数类型与参数单位、Case、ScoreProfile、ReferenceBinding、`executionOutcomePolicy`、指标集合、MetricDefinition 和结果单位。任一项不满足即产生 `error`，并禁止输出差值和排序。

`evaluatorVersion`、数值类型和数值环境差异记录为 `finding`，不单独判为不兼容；调用方可在更严格的后续策略中把这些差异升级为门槛。Case/Profile 的省略默认值与显式同值默认项按本策略视为等价；Artifact 中“未提供语义”和“明确声明未知”仍是不同输入事实，不得在内容标识中折叠。

v0.2 的导入式 `ComparisonSpec` 冻结的是两个 Subject 的输出及共同评估条件，尚不包含上游算法输入和 `ParameterSet`；因此它能证明“两个输出在同一 Case 下可比较”，不能单独证明“两个算法使用了同一输入和参数”。完整 Case D 需要后续 Experiment/Runner 把这两类身份一并冻结。

## 9. 证据与可复现性

每个结果至少保存：

- 原始点 Artifact 的内容标识；
- Profile、ReferenceBinding 和 Case 版本；
- 指标算法、参数、数值类型和容差；
- 任何转换后的 Artifact 及完整派生链；
- MetricResult 状态和异常样本索引；
- 能力解析结果、完整能力 ID 和对应策略版本；
- 对应 Claim 的证据等级。

典型证据口径：

- 解析构造的测试序列可支持 `Exact` 声明；
- 与独立实现或高精度结果交叉一致可支持 `Validated`；
- 算法运行耗时和真实采样数据通常是 `Observed`；
- 若某个距离或边界使用严格区间方法证明，可声明 `Certified`。

证据等级属于具体 Claim，不能给整个文件笼统盖章。

## 10. 首版测试族

### 10.1 有效输入

- 单点；
- 两点线段；
- 等间距直线；
- 不等间距直线；
- 二维圆弧和闭合圆；
- 三维螺旋或空间折线；
- 含合法相邻重复点的序列；
- 带严格递增时间戳的序列。

### 10.2 失败与边界输入

- 空序列；
- 维数混合；
- NaN/Inf；
- 全部点相同；
- 非单调时间；
- 时间长度不匹配；
- Profile 单位或坐标系冲突；
- 参考维数不一致；
- 未声明对应策略却请求逐点误差。

### 10.3 变形关系

在指标适用时检查：

- 平移不改变步长、长度和转角；
- 刚体旋转不改变欧氏距离类指标；
- 一致尺度变换按比例改变长度，不改变转角；
- 反转序列保留无向长度，但可能改变定向/时间相关结论；
- 重采样可能改变逐点误差和离散统计，因此必须形成新 Artifact。

### 10.4 规范一致性样例

下列结果是首版实现的确定性验收基线；它们使用确定性的 `artifact-import` Runner，数值比较使用 MetricDefinition 声明的容差：

| 样例 | 输入与必选指标 | 必须得到的结果 |
|---|---|---|
| `OPS-C01` | `[(0,0),(3,4)]`，无 semantics；必选 `point.count`、`path.length.open`、`closure.gap` | `ExecutionStatus = Succeeded`；结果为 `2`、`5 coordinate-unit`、`5 coordinate-unit`；`CaseOutcome = Passed` |
| `OPS-C02` | 同上，必选 `path.length.closed` | `ExecutionStatus = Skipped`；指标为 `InsufficientContext`；`CaseOutcome = Inconclusive` |
| `OPS-C03` | 同上且 `closed=false`，必选 `path.length.closed` | `ExecutionStatus = Skipped`；指标为 `NotApplicable`；该 Case/指标组合无效，`CaseOutcome = Invalid` |
| `OPS-C04` | 同上且 `closed=true`，必选 `path.length.closed` | `ExecutionStatus = Succeeded`；结果为 `10 coordinate-unit`；`CaseOutcome = Passed` |
| `OPS-C05` | 无单位输入，必选阈值 `path.length.open <= 6 mm` | `ExecutionStatus = Skipped`；阈值指标为 `InsufficientContext`；`CaseOutcome = Inconclusive` |
| `OPS-C06` | ReferenceBinding 完整，但 Evaluator 不实现其已绑定策略；该参考指标为必选 | `ExecutionStatus = Skipped`；指标为 `UnsupportedCapability`；`CaseOutcome = Unsupported` |

此外必须各有一个可重放 fixture 覆盖 Case A、B、C、D；fixture 冻结输入内容、Case、能力清单、策略版本、期望公共状态和期望数值/容差。

## 11. `R1` 阶段门

有序离散点切片只有同时满足以下条件才算完成：

- 单独输入点序列即可得到诚实的最低层评估；
- 增加单位、参考或时间后，能力按契约增加；
- 缺失上下文不会被自动猜测；
- 原始输入始终保留，任何清洗或变换可追溯；
- 支持单序列、参考比较、带时间序列和 A/B 四类可复现 Case；
- §10.4 的六个一致性样例及四类 fixture 全部通过；
- 两次确定性重放得到一致结果；
- 每个失败都能分别落到 `ExecutionStatus`、`MetricStatus`、`CaseOutcome` 和领域失败明细；
- 不修改 Axiom Core 即可注册本领域包；
- 五轴领域包可以通过显式 Adapter 消费或产生相关序列。

## 12. 与五轴领域包的边界

有序点序列不自动等于刀位轨迹、刀轴轨迹或 M5 插补命令。要进入 `FiveAxisTrajectoryPack`，必须显式说明：

- 点表示工件空间位置、旋转轴位置还是其他量；
- 坐标系、单位和方向；
- 点间同步与参数/时间语义；
- 姿态、进度、源段和机器配置如何绑定；
- 转换是否保留了领域包所需不变量。

反过来，五轴领域包可以把 M1–M5 中明确为二维/三维欧氏坐标的位置分量或采样结果，通过 Adapter 暴露为本领域包 Artifact，用于通用结构检查和基础统计，但五轴合法性仍由五轴领域包负责。含线性轴与旋转轴混合单位的关节向量不能直接冒充欧氏点；它需要由五轴领域包保留为专有 Artifact，或在未来接入带显式分量度量的向量序列领域包。

---

上一篇：[Axiom 通用评估框架规范](Axiom%20通用评估框架规范.md)  
下一篇：[FiveAxisTrajectoryPack — CNC 五轴数学参考系统全景规范](CNC%20数学参考系统全景规范.md)  
返回：[文档入口](README.md)
