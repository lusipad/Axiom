# Axiom 五轴物理模型与现实对齐规范

> 状态：R4 v1 / Windows synthetic SIL 参考实现
> 更新日期：2026-08-12
> 适用范围：Math F4 离散命令、R3 只读观测与 R4 轴空间物理模型验证

## 1. 目的与阶段边界

本规范定义 Axiom 如何在同一 Case 中保留并比较三类彼此独立的证据：

1. F4 `M5DiscreteCommand` 提供的数学指令；
2. `PhysicalModel` 产生的仿真响应；
3. R3 `MachineTelemetryTrace` 提供的设备格式观测。

R4 v1 的目标是闭合可重放、可证伪的 synthetic SIL 模型验证链。它验证模型合同、校准、时间/通道对齐、独立 holdout、单位分组残差和适用域状态；它不证明真实机床、控制器、加工过程或安全性。

```text
F4 M5 mathematical command
  + PhysicalModel / calibration
  -> PhysicalResponseTrace (simulation)
  + R3-compatible raw observation
  + explicit time/channel alignment
  -> command / simulation / observation residual evidence
  -> case-scoped claims with applicability and credibility boundary
```

本阶段永远禁止直接输出：

- `DeviceSafe`、`ProcessSafe` 或 `safe-to-run`；
- 设备启动、写参数、程序传输或在线补偿许可；
- synthetic replay 等同于真实设备真值；
- 模型在未验证设备、轨迹、速度、温度或加工条件上的泛化保证；
- 把数学无碰撞或低残差升级为设备安全批准。

## 2. 冻结标识与角色

| 概念 | 标识/角色 |
|---|---|
| DomainPack | `five-axis.domain-pack@6` |
| 主 Artifact | `five-axis.physical-response-trace` |
| Evaluator | `five-axis-physical-model-evaluator@1` |
| Runner | `five-axis-physical-simulation@1` |
| 候选模型族 | `five-axis.independent-first-order-axis-model@1` |
| 数值积分 | exact ZOH |
| 校准策略 | 固定网格 tau 搜索 + 解析 bias，版本化且确定性 |
| 首个证据源 | Windows JSON `synthetic-replay` |

一次 Run 的唯一主 Artifact 是 `PhysicalResponseTrace`。它是模型执行产生的仿真响应，所以通用 Core 将 Observation source 记录为 `ExecutedSubject`。以下对象是有类型支持上下文，不得与主 Artifact 混为一个无类型 mega-payload：

- `PhysicalModelDefinition`；
- calibration 与 validation 两组 `M5DiscreteCommand`；
- 两组 R3 `MachineObservationRequest` 及 raw trace；
- `PhysicalRunAlignment`；
- calibration snapshot 和内容身份。

支持对象的内容身份必须进入 provenance `contextHashes`。模型/Profile 来源投影为 `Profile`，时间/坐标/通道转换投影为 `Adapter`，仿真响应投影为 `Artifact`，执行/复核能力投影为 `Evaluator`；不得为 R4 扩展 Core 的 `CapabilityResolution.source` 枚举。

## 3. 物理模型合同

### 3.1 必填声明

每个 `PhysicalModelDefinition` 至少声明：

- model、schema、算法和 calibration identity；
- 状态、输入、输出及轴 ID；
- 每个轴的 unit family 和单位；
- 时间尺度、采样假设与积分方法；
- 参数来源和标定版本；
- 适用设备、控制器、轨迹/速度或其他工况边界；
- 未建模因素；
- 与 Observation 的对齐策略 ID。

模型适用域不得由 Evaluator 根据数据表现临时放宽。超出设备身份、采样周期、速度、轨迹族或其他冻结边界时，应返回 `NotApplicable`、`InsufficientContext` 或 `Inconclusive`，不能外推正向 Claim。

### 3.2 v1 一阶轴模型

对每个已辨识轴 (i)：

\[
\dot{x}_i(t) = \frac{u_i(t)-x_i(t)}{\tau_i},
\qquad
y_i(t)=x_i(t)+b_i
\]

其中：

- (u_i) 是 M5 指令位置；
- (x_i) 是动态状态；
- (y_i) 是仿真观测位置；
- \(\tau_i>0\) 是时间常数；
- (b_i) 是静态偏置。

在每个采样区间内采用零阶保持，必须使用解析更新：

\[
x_{k+1}=u_k+(x_k-u_k)\exp(-\Delta t/\tau_i)
\]

不得用未声明的 Euler 步长替代 exact-ZOH，也不得用平滑或插值改变原始 R3 frame。

### 3.3 首版未建模因素

v1 必须明确列出至少以下未建模因素：

- 摩擦、反向间隙和迟滞；
- 饱和、限幅与控制器内部前馈；
- 轴间耦合与结构柔性；
- 几何装配误差；
- 热漂移；
- 切削力、刀具/工件过程和磨损；
- 传感器误差与外部计量不确定度。

这些因素进入残差解释和后续模型版本，不能从一次低残差中被视为不存在。

## 4. 数据、身份与不变量

### 4.1 `PhysicalResponseTrace`

主 Artifact 至少包含：

- artifact/schema/response identity；
- source model 与 calibration content identity；
- source validation M5 command content identity；
- sample period、轴定义和有序响应样本；
- simulation method、numeric environment 和 provenance；
- 明确的 synthetic / SIL 边界。

Evaluator 必须按冻结模型和 M5 指令独立重放 response trace。修改任一响应值、模型参数、source identity、采样周期或 method 都必须导致内容身份或重放失败；Artifact 自报的结论不能直接成为 Claim。

### 4.2 `PhysicalEvidencePair`

每个 pair 显式绑定：

- pair identity 与 role（calibration 或 validation）；
- F4 M5 command；
- R3 `MachineObservationRequest`；
- command/trace/R3 report 或 lineage 内容身份；
- 设备与工况身份。

R4 必须重新验证 M5 内容身份、R3 raw trace hash、paired lineage 和冻结的七项 F4 数学 Claim。R3 正向 Claim 只证明 raw/context/lineage 前置条件；它不替代 R4 的逐样点数值对齐。

### 4.3 校准—验证隔离

calibration 与 validation 至少必须满足：

- pair ID 不同；
- M5 command content identity 不同；
- raw trace content identity 不同。

任一身份复用都属于 `CalibrationValidationLeakage`。它必须阻断 calibration traceability、fit 与 validation 正向 Claim，不得作为“数据较少”的可接受降级。

### 4.4 Raw 不可变

R4 不得修改、排序、补点或覆盖 R3 raw frames。派生的对齐 sample、仿真值与残差只进入 R4 Artifact、typed analysis、Metric details 或 Evidence，并保存其来源内容身份。

## 5. 时间、轴通道和单位对齐

### 5.1 `PhysicalRunAlignment`

R4 数值对齐至少冻结：

- alignment policy identity；
- command t=0 对应的设备时间锚点；
- 允许的最大时间差与覆盖策略；
- 每个 command axis 到 R3 scalar channel 的显式映射；
- channel 单位与 axis unit family；
- coordinate alignment identity 与 calibration status；
- 是否允许插值；v1 必须为 `false`。

R3 `ClockMapping` 和 `CoordinateAlignment` 的存在不等于这些数值规则已经执行。R4 Evaluator 必须逐点匹配并报告 coverage、最大时间差和所有未匹配原因。

### 5.2 Gap 与错误对齐

- missing clock anchor、时间超差、序列 gap 或缺 channel 时不得插值；
- coverage 不完整时，依赖完整序列的残差和 fit Claim 为 `Inconclusive`；
- channel/axis/unit 不一致属于无效上下文；
- 未校准坐标只能保留 raw Observation，不得产生现实 fit 正向 Claim。

### 5.3 单位隔离

线性轴使用 `mm`，旋转轴使用 `rad`。R4 必须分别报告：

- linear math–observation / simulation–observation residual；
- rotary math–observation / simulation–observation residual；
- 各自 max、RMSE 和适用轴集合；
- 可选的、通过显式 scale 归一化后的无量纲指标。

禁止直接相加或平均 `mm` 与 `rad`。没有旋转轴激励时，rotary 指标为 `NotApplicable` / `InsufficientExcitation`，不能返回伪造的 0 误差总分。

## 6. 校准、验证与残差分解

### 6.1 确定性校准

v1 对每个满足最小 excitation span 的轴：

1. 按冻结网格枚举 tau；
2. 对每个 tau 以同一初始化和 exact-ZOH 重放；
3. 解析计算使平方误差最小的 bias；
4. 以 RMSE、tau 和固定序列次序完成确定性 tie-break；
5. 记录搜索边界、步长、work units 和 calibration content identity。

Artifact 声明的参数必须与 Evaluator 独立重算一致。未充分激励的轴不得输出拟合参数。

### 6.2 独立 SIL oracle

仓库 reference fixture 的观测由与候选不同的两级滞后 oracle、静态偏置和确定性扰动生成，并标记 `synthetic-replay`。候选一阶模型不得直接用于生成自己的 calibration 或 validation 观测。

此做法只证明模型验证链能面对结构 mismatch 并在 holdout 上被证伪；它不增加真实设备证据等级。

### 6.3 三段残差

对同一个对齐 sample 和同一单位 family，必须同时计算：

- (r_{mo}=y_{math}-y_{obs})；
- (r_{ms}=y_{math}-y_{sim})；
- (r_{so}=y_{sim}-y_{obs})。

残差分解必须按固定符号满足：

\[
r_{mo}=r_{ms}+r_{so}
\]

报告 closure error 与数值容差。任何单位混用、样本集合不同或符号不闭合都阻断 decomposition Claim。

### 6.4 Holdout 判据

fit Claim 只能使用 validation pair。至少比较 validation 上的 math–observation RMSE 与 simulation–observation RMSE，并按 Case 冻结 improvement threshold。calibration 上的良好拟合只能证明 calibration replay，不得替代 holdout。

## 7. Metric、Claim 与 Evidence

### 7.1 最小 Metric 集

R4 v1 至少覆盖：

- simulation contract/replay validity；
- calibration traceability 与 leakage gate；
- alignment coverage；
- linear residual max/RMSE（`mm`）；
- rotary residual max/RMSE（`rad`，有激励时）；
- residual decomposition closure；
- holdout improvement ratio；
- applicability / excitation status。

硬门先于质量：身份、raw integrity、alignment、unit、leakage 或 applicability 未通过时，不得用低 RMSE 抵消。

### 7.2 允许的 Claim

R4 v1 可以定义并按 Metric 投影：

- `five-axis.physical-model-contract-valid-claim@1`；
- `five-axis.physical-calibration-traceable-claim@1`；
- `five-axis.physical-alignment-valid-claim@1`；
- `five-axis.physical-residual-decomposition-closed-claim@1`；
- `five-axis.physical-model-fit-within-tolerance-claim@1`；
- `five-axis.physical-model-reality-validated-claim@1`。

只要 observation source 是 `synthetic-replay`，最后一项必须为 `Inconclusive`。前五项即使 Supported，也仅在声明的 synthetic SIL Case 和 applicability 内成立。

### 7.3 Evidence 等级

- 数学 F4 Claim 保留其原始证据含义；R4 不重定义数学真值。
- 仿真、历史运行和设备观测在通用证据表中最高为 `Observed`。
- synthetic SIL 必须在 method/details 中明确标注，不得用模糊的 `Validated` 文案冒充现实验证。
- 真实 reality Claim 至少需要独立 holdout 的 `controller-export` 或 `device-read` paired telemetry；更强结论还需要外部计量、不确定度和代表性工况覆盖。

## 8. 场景与阶段状态

最低场景矩阵：

| 场景 | 预期 |
|---|---|
| in-domain synthetic SIL | 合同、校准、对齐、分解和 holdout fit 可支持；reality 为 Inconclusive |
| model mismatch | holdout fit 被 Refuted，证明反例能击穿模型 |
| time alignment mismatch | coverage 不闭合；相关 Claim Inconclusive/Refuted |
| calibration/validation leakage | Invalid，禁止 fit 正向 Claim |
| insufficient axis excitation | 受影响轴 NotApplicable/InsufficientExcitation |
| missing coordinate context | raw 可保留，物理 fit Inconclusive |

R4 阶段报告必须拆分：

```text
syntheticContractStatus: Passed | Failed
realityValidationStatus: Open | CaseScopedPassed | Failed
```

仓库 reference fixture 只能得到 `syntheticContractStatus=Passed` 与 `realityValidationStatus=Open`。只有加入真实、独立、可追溯的 holdout Case 后，才能把对应 Case 标记为 `CaseScopedPassed`；不能从单个设备/工况外推全局 R4 已闭合。

## 9. API 与网页工作台

Windows 参考实现提供：

- `GET /api/v1/physical/r4/manifest`；
- `GET /api/v1/physical/r4/scenarios`；
- `GET /api/v1/examples/physical-r4?scenarioId=...`；
- `POST /api/v1/runs/evaluate`。

Physical R4 工作台必须展示：

- `MODEL VALIDATION / NOT DEVICE SAFE`；
- `SYNTHETIC SIL / REALITY VALIDATION OPEN`；
- 模型方程、状态、输入、输出、exact-ZOH 和 applicability；
- calibration / validation 数据身份与 isolation gate；
- command / simulation / observation 三条曲线；
- 按 `mm` / `rad` 分组的指标、excitation 和残差分解；
- Claim、Evidence、Inconclusive/Refuted 原因。

页面不得提供设备写入、启动、控制、参数应用或“确认上机”控件。

## 10. Windows 验收门

1. `five-axis.domain-pack@6` 经通用 `DomainRuntimeBinding` 执行，Core 无 R4 special-case。
2. 主 Artifact 为 `PhysicalResponseTrace`，Observation source 为 `ExecutedSubject`；R3 raw trace 保持不可变支持对象。
3. 两次重放得到相同模型参数、response/analysis/report/RunBundle 内容身份。
4. M5、F4 七项 Claim、R3 raw/context/lineage、模型、标定和 alignment identity 均被重验。
5. 校准/验证三重身份隔离，leakage 反例会阻断正向 Claim。
6. 不插值、不静默排序、不混合 `mm/rad`，未激励轴不造参数。
7. independent SIL oracle 与候选模型结构不同；holdout fit 可通过，mismatch 反例可推翻。
8. Python、CLI、HTTP 与网页使用同一 RunSpec 和状态语义。
9. Windows AMD64 + CPython 3.12.10 的 Python/Web 测试、构建、wheel 内容和安装后 API/UI smoke 全部通过。
10. 仓库、API、RunBundle、网页和发布说明均不得从 synthetic 结果产生设备/过程安全或 reality-validated 正向声明。

## 11. 后续接口

R4 下一步不是扩大 synthetic fixture 数量，而是加入真实、只读、paired 的控制器导出或设备采集，并冻结设备、固件、控制周期、工况、校准、不确定度和独立 holdout。旋转轴模型只有在 F4 合法命令提供足够旋转激励后才能辨识。

R5 学习模型只能消费经过本规范身份、单位、对齐、leakage 和 applicability 门的残差数据。R6 参数建议与 R7 受控执行必须继续使用独立硬门、权限、限幅、停止和审计合同；R4 的低残差不能成为自动写设备的授权。

## 12. 研究与标准依据

- [ISO 230-2:2014](https://www.iso.org/obp/ui/en/)：机床单轴定位精度与重复定位测试。
- [ISO 230-4:2022](https://www.iso.org/standard/79155.html)：圆测试与数控机床轮廓性能。
- [ISO 230-3:2020](https://www.iso.org/standard/73291.html)：热效应测试。
- [ISO/TR 230-9:2005](https://www.iso.org/standard/39165.html)：ISO 230 测试不确定度估计。
- [ISO 23247-1:2021](https://www.iso.org/standard/75066.html) 与 [ISO 23247-4:2021](https://www.iso.org/standard/78745.html)：制造数字孪生原则与信息交换。
- [NIST：Data Requirements for a Digital Twin of a CNC Machine Tool](https://www.nist.gov/publications/data-requirements-digital-twin-cnc-machine-tool)：CNC 数字孪生的数据目标、范围与质量要求。
- [NIST：Credibility Consideration for Digital Twins in Manufacturing](https://www.nist.gov/publications/credibility-consideration-digital-twins-manufacturing)：制造数字孪生的 VVUQ 与可信度边界。
- [NASA-STD-7009B (2024)](https://standards.nasa.gov/sites/default/files/standards/NASA/B/1/NASA-STD-7009B-Final-3-5-2024.pdf)：模型与仿真的 verification、validation、uncertainty 与 credibility 规则。
- 轴/轮廓动态误差与补偿研究：DOI [`10.1016/j.ijmachtools.2012.11.002`](https://doi.org/10.1016/j.ijmachtools.2012.11.002)、[`10.1109/TMECH.2021.3068354`](https://doi.org/10.1109/TMECH.2021.3068354)、[`10.3390/electronics13061143`](https://doi.org/10.3390/electronics13061143)。
