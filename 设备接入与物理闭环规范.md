# Axiom 设备接入与物理闭环规范

> 状态：R3 v1 / Windows 只读参考实现
> 更新日期：2026-08-12
> 适用范围：R3 设备只读观测，以及进入 R4 物理模型前的数据边界

## 1. 目的与边界

本规范定义 Axiom 如何接收设备、控制器、驱动器或仿真器产生的运行观测，并把它们保存为可审计、可对齐、可追溯的数据。R3 的目标是建立可信采集链，不是控制设备，也不是证明设备或加工过程安全。

首个参考源是 `axiom.windows-file-telemetry-source@1`：在 Windows 上读取已经落盘的 JSON capture。该源可以承载控制器导出、仿真器导出或合成回放，但首版不连接网络、不轮询设备、不调用厂商 SDK，也不包含任何写入、启动或联锁旁路能力。

R3 永远不得把以下结论作为本阶段输出：

- `DeviceSafe`；
- `ProcessSafe`；
- `safe-to-run` 或上机许可；
- 未经标定的控制器坐标等同于测量真值；
- 由缺失样本插值得到的值等同于原始观测。

## 2. 分层关系

```text
Windows JSON capture（只读）
  → MachineTelemetryTrace（原始 Artifact）
  → DeviceProfile（数据项、单位和设备身份）
  → ClockMapping / CoordinateAlignment（独立上下文）
  → MachineRunLineage（paired 或 unpaired）
  → Machine Observation Evaluator
  → MetricResult / Claim / Evidence / RunBundle
```

原始 Artifact 与派生对齐必须分开保存。对齐、gap 诊断、指标和推断值只能进入支持对象、结果或 Evidence，不得改写原始 frame。

## 3. 冻结标识

| 概念 | 标识 |
|---|---|
| DomainPack | `machine-observation.domain-pack@1` |
| 主 Artifact type | `machine.telemetry-trace` |
| Evaluator | `machine-observation-evaluator@1` |
| Runner | `machine-trace-import@1` |
| Windows 文件源 | `axiom.windows-file-telemetry-source@1` |

Runner 只导入已存在的 capture。若未来增加在线 GET、目录 watcher、OPC UA、MTConnect 或厂商 SDK，必须使用新的版本化 source/runner 合同，并保持本规范的原始数据、只读权限和谱系不变量。

`machine-trace-import@1` 必须同时出现在 DomainPack 的 `runnerIds` 和 `importRunnerIds` 中；因此通用 Core 将 Observation 来源记录为 `ImportedArtifact`，不按设备领域 ID 编写特例。

## 4. 核心对象

### 4.1 `MachineTelemetryTrace`

原始遥测主 Artifact 至少包含：

- capture、source 和 schema 身份；
- 设备身份；
- capture 开始/结束时间或设备时间范围；
- 有序 raw frames；
- 每个数值数据项的单位和质量状态；
- 报警、状态和 gap 等源端显式事件；
- 可选的 vendor metadata。

Evaluator 不得为方便计算而静默排序 raw frames。设备时间逆序属于无效观测；相同时间戳只有在源合同显式允许并能保持输入顺序时才可接受。

### 4.2 `DeviceProfile`

Profile 至少冻结：

- `deviceId`、`manufacturer`、`machineModel` 或仿真身份、可选 `firmwareVersion` 与必选 `exportVersion`；
- 数据项 ID、kind、unit 和坐标语义；
- `allowedReadOnlyOperations`；R3 v1 只能且必须为一次 `file-import`；
- 可选的 calibration identity。

Trace 的设备身份必须和 Profile 一致。数值 sample 若没有单位，不得由 Evaluator 猜测。

### 4.3 `ClockMapping`

时钟映射独立记录设备时钟到主机时钟的 `mappingMethod`、设备/主机锚点、偏移和漂移或误差界。v1 方法只允许 `fixed-offset`、`synchronized-export` 或 `vendor-declared`。缺少映射时，原始导入仍可成立，但依赖跨时钟对齐的指标必须返回 `InsufficientContext`，对应 Claim 为 `Inconclusive`。

### 4.4 `CoordinateAlignment`

坐标对齐至少绑定来源坐标系、目标坐标系、变换或标定标识、`effectiveAt` 和 `sourceKind`。v1 来源只允许 `calibration-record`、`vendor-declared` 或 `synthetic-reference`。缺少或身份不匹配时，不得把控制器坐标作为测量真值；依赖物理坐标比较的指标必须返回 `InsufficientContext`。

Run provenance 必须通过 `contextHashes` 分别冻结 `deviceProfile`、`lineage`，以及存在时的 `clockMapping` 和 `coordinateAlignment`；完整 `requestHash` 不能替代这些可独立审计的支持对象身份。

### 4.5 `MachineRunLineage`

谱系状态只有：

- `paired`：关联到一个可重验的 Reference/Benchmark Run；
- `unpaired`：历史运行没有可信数学来源，但仍可保存和审计。

`paired` 必须同时绑定：

1. baseline `RunBundle` 内容身份；
2. 来源 M5 `DiscreteCommand` 内容身份；
3. F4 冻结的七项数学 Claim，且均为 `Supported`：
   - `five-axis.geometry-valid-claim@1`；
   - `five-axis.task-geometry-collision-free-claim@1`；
   - `five-axis.kinematically-feasible-claim@1`；
   - `five-axis.configuration-collision-free-claim@1`；
   - `five-axis.continuously-feasible-claim@1`；
   - `five-axis.interval-certified-claim@1`；
   - `five-axis.model-collision-free-claim@1`。

`unpaired` 禁止携带伪造的 baseline 或 M5 identity。未配对不是导入失败，但它不能产生“谱系完整”的正向 Claim。

## 5. 只读 source 合同

首版 source 只接受一个显式文件输入并完成以下动作：

1. 以只读方式打开文件；
2. 读取 bytes 并计算内容身份；
3. 解析 JSON capture；
4. 记录 source、文件内容身份、导入状态和失败原因；
5. 把 capture 作为原始 Artifact 交给通用 Run 执行链。

Trace 内容身份覆盖规范化后的完整 Artifact，只排除自引用字段 `captureReceipt.traceContentHash`；修改 frame、receipt、设备身份或 vendor metadata 均必须导致重验失败。源文件 bytes 的 SHA-256 由只读 loader 另行返回为 `sourceFileHash`，两者不得混用。

首版 source/runner 不得包含下列 operation：

- 写参数或覆盖配置；
- 上传、下载或传输 CNC 程序到设备；
- cycle start、feed hold、reset、jog 或轴运动；
- 修改 offset、补偿、坐标系或联锁；
- 对设备发起任何协议写请求。

若 capture 声称它由本平台执行了上述 operation，必须被拒绝为非只读合同，不得降级为告警后继续。

## 6. 指标、Claim 与 Evidence

R3 只允许发布数据可信性 Claim：

| Claim | 含义 | 正向条件 |
|---|---|---|
| `machine-observation.raw-integrity-claim@1` | 原始 capture 可验证且未被派生值覆盖 | schema、内容身份、顺序、单位和设备身份有效 |
| `machine-observation.read-only-capture-claim@1` | 导入链没有设备写操作 | source/receipt/operation 均满足只读合同 |
| `machine-observation.lineage-complete-claim@1` | paired 谱系完整 | baseline、M5 和七项 F4 Claim 均闭合 |
| `machine-observation.clock-aligned-claim@1` | 可进行声明范围内的跨时钟对齐 | mapping 身份和误差上下文完整 |
| `machine-observation.coordinate-context-claim@1` | 坐标解释上下文完整 | frame、标定/变换身份与适用范围完整 |

所有 R3 Evidence 的最高等级是 `Observed`。合成 fixture 必须显式标记为 `synthetic-replay`；它能证明实现合同和确定性，不能冒充真实设备证据。

## 7. 状态与失败语义

| 条件 | MetricStatus | CaseOutcome | 要求 |
|---|---|---|---|
| 合法只读 trace | `Computed` | 由 Case 聚合 | 保留 raw Artifact |
| 缺时钟映射 | `InsufficientContext` | `Inconclusive` 或仅影响 optional metric | 不猜映射 |
| 缺坐标/标定上下文 | `InsufficientContext` | `Inconclusive` 或仅影响 optional metric | 不发布坐标正向 Claim |
| unpaired 历史导入 | lineage 为不可计算或不适用 | 可由只读导入 Case 接受 | 不伪造 baseline |
| 时间戳逆序、单位缺失或身份不匹配 | `InvalidObservation` | `Invalid` | 不静默修复 |
| 出现未声明 gap | 连续性相关指标不可计算 | `Inconclusive` | 不插值补点 |
| source/trace 包含写操作 | `InvalidObservation` | `Invalid` | 结构化失败并拒绝执行 |

## 8. Machine Lab 与 API

首版 Machine Lab 使用：

- `GET /api/v1/machine/r3/manifest`；
- `GET /api/v1/machine/r3/scenarios`；
- `GET /api/v1/examples/machine-r3?scenarioId=...`；
- `POST /api/v1/runs/evaluate`。

页面必须永久显示 `READ ONLY / NOT DEVICE SAFE`，并分开展示：

- Device/Profile；
- raw frames 和 source receipt；
- clock/coordinate alignment；
- paired/unpaired lineage；
- gap、报警和无上下文原因；
- Metric、Claim 和 Evidence。

页面不得提供写参数、启动、暂停、复位、程序传输或联锁操作控件。

## 9. R3 v1 验收门

1. 新 DomainPack 经通用 `DomainRuntimeBinding` 执行，Core 无设备 special-case。
2. 同一 capture 两次导入产生相同 Artifact、Report 和 RunBundle 内容身份。
3. `Observation.artifact` 逐字段保留 raw trace 和 vendor metadata。
4. paired 场景闭合 baseline、M5 和七项 F4 Claim；unpaired 场景显式保持未配对。
5. 缺 clock/coordinate context 时只产生 `InsufficientContext` / `Inconclusive`，不猜值。
6. gap 不被插值，逆序、身份不匹配和写操作产生结构化失败。
7. CLI、HTTP、Python 与 Machine Lab 使用同一 RunSpec 和结果语义。
8. Windows AMD64 + CPython 3.12.10 的测试、构建、wheel 安装后 API/UI smoke 全部通过。
9. 代码、API 和 UI 中不存在设备写入路径，也不产生 `DeviceSafe` / `ProcessSafe`。

## 10. 后续阶段接口

R4 可以消费本规范冻结的 raw Artifact、对齐上下文和谱系，增加 PhysicalModel、标定、仿真与残差 Evidence；不得覆盖 R3 原始 Observation。R5 及以后只有在数据授权、保留策略和跨设备语义稳定后才能把这些 Run 纳入训练集。

实时只读采集、厂商协议、签名 capture、边缘代理和外部测量系统属于后续 source adapter。任何写入设备的能力至少属于 R7 受控执行面，必须另行建立权限、限幅、停止、回滚和审计合同。
